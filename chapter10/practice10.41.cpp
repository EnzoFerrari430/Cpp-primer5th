#include <iostream>
using namespace std;
/*
    replace(beg,end,old_val,new_val);
    在迭代器beg，end指示的范围中将值old_val替换成new_val
    replace(beg,end,pred,new_val);
    在迭代器beg，end指示的范围中将符合pred条件的值替换成new_val
    replace_copy(beg,end,dest,old_val,new_val);
    在迭代器beg，end指示的范围中将old_val替换成new_val再拷贝至dest中
    replace_copy_if(beg,end,dest,pred,new_val);
    在迭代器beg，end指示的范围中将符合pred的值替换成new_val，再拷贝至dest中


*/
int main()
{



    return 0;
}
