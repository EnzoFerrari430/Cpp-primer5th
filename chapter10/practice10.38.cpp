#include <iostream>
using namespace std;
/*
    10.38
        输入迭代器           == != ++ * ->
        输出迭代器           ++ *
        前向迭代器           == != ++ * ->
        双向迭代器           == != ++ * -> --
        随机访问迭代器       ==  !=  ++  *  ->  --  >  >=  <  <=  +  +=  -  -=  iter[n]  *(iter[n])

    10.39
        list上的迭代器属于双向迭代器  list双向链表
        vector  随机访问迭代器

    10.40
        copy        参数1 2 输入迭代器     参数3输出迭代器
        reverse     双向迭代器
        unique      前向迭代器
*/
int main()
{



    return 0;
}
