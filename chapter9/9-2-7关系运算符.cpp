#include <iostream>
#include <string>
#include <vector>
#include <list>
using namespace std;
/*
    所有容器都支持相等运算符(==和!=)
    除了无序关联容器外的所有容器都支持关系运算符(>、>=、<、<=)关系运算符左右2边必须是相同类型的容器，且必须保存相同类型的元素
    关系运算符的比较方式与string类似
    只有当其元素类型也定义了相应的比较运算符时，我们才可以使用关系运算符来比较2个容器
*/
int main()
{


    return 0;
}
