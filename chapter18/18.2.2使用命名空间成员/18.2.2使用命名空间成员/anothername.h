#pragma once
#include <iostream>

/*
命名空间的别名使得我们可以为命名空间的名字设定一个短的多的同义词。

例:
	namespace cplusplus_primer{};

	可以为其设定一个短的多的同义词
	namespace primer = cplusplus_primer;

using 声明
	一条声明语句一次只引入命名空间的一个成员
	例:  using std::cin;

using 指示
	使得某个特定的命名空间中所有的名字都可见
	例:  using namespace std;
*/